import { Character } from '../baseCharacter.js'

export class Ranger extends Character {
    constructor() {
        super();
    }

    test2() {
        if (character.ctype === "ranger") {
        game_log(`ADDITIONALLY I AM A ${this.ctype} of number ${Math.random()}`)
        }
    }

}